import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.lunarpuffintwirl',
  appName: 'lunar-puffin-twirl',
  webDir: 'dist'
};

export default config;
